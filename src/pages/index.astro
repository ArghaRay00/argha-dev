---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Socials from "@/components/Socials.astro";
import LinkButton from "@/components/LinkButton.astro";
import Card from "@/components/Card.astro";
import getSortedPosts from "@/utils/getSortedPosts";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";
import { SITE } from "@/config";
import { SOCIALS } from "@/constants";

const posts = await getCollection("blog");

const sortedPosts = getSortedPosts(posts);
const featuredPosts = sortedPosts.filter(({ data }) => data.featured);
const recentPosts = sortedPosts.filter(({ data }) => !data.featured);
const allPosts = [...featuredPosts, ...recentPosts];
---

<Layout>
  <Header />
  <main id="main-content" data-layout="index" class="app-layout">
    <!-- Intro -->
    <section class="pt-12 pb-10">
      <p class="text-foreground/60 leading-relaxed max-w-lg">
        Software engineer at <strong class="text-foreground font-medium">Motorola Solutions</strong>, Bangalore.
        Nine years in production backend systems — Java, C#, TypeScript, Python.
        Currently building agent infrastructure on the side: tool orchestration, knowledge systems, and a
        <a href="https://jishu.argha.dev/chat" target="_blank" class="text-accent hover:underline underline-offset-4">24/7 autonomous agent</a>
        with its own cognitive architecture.
      </p>
      {
        SOCIALS.length > 0 && (
          <div class="mt-4 flex items-center gap-2">
            <Socials />
          </div>
        )
      }
    </section>

    <!-- Posts -->
    {
      allPosts.length > 0 && (
        <section class="border-t border-border pt-8 pb-6">
          <h2 class="text-xs font-mono uppercase tracking-widest text-foreground/35 mb-8">Writing</h2>
          <ul>
            {allPosts.map(
              (data, index) =>
                index < SITE.postPerIndex && <Card variant="h3" {...data} />
            )}
          </ul>
        </section>
      )
    }

    {
      allPosts.length > SITE.postPerIndex && (
        <div class="my-8 text-center">
          <LinkButton href="/posts/">
            All Posts
            <IconArrowRight class="inline-block rtl:-rotate-180" />
          </LinkButton>
        </div>
      )
    }
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>
